{
  "name": "monthaven-monorepo",
  "private": true,
  "workspaces": ["apps/web"],
  "scripts": {
    "lint": "npm run lint -w apps/web",
    "typecheck": "npm run typecheck -w apps/web",
    "build": "npm run build -w apps/web"
  }
}
