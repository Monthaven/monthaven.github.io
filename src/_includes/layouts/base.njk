<!doctype html>
<html lang="{{ site.lang }}">
<head>
{% include "partials/head.njk" %}
</head>
<body>
<a class="skip-link" href="#main">Skip to content</a>
{% if env.staging %}<div class="staging-flag" role="status">STAGING BUILD, not the live site. Not indexed. Forms post to the real intake.</div>{% endif %}
{% include "partials/header.njk" %}

{% if breadcrumbs and breadcrumbs.length > 1 %}
<nav class="breadcrumb" aria-label="Breadcrumb">
  <div class="wrap">
    <ol>
      {% for b in breadcrumbs %}
      <li>{% if loop.last %}<span aria-current="page">{{ b.name }}</span>{% else %}<a href="{{ b.url }}">{{ b.name }}</a>{% endif %}</li>
      {% endfor %}
    </ol>
  </div>
</nav>
{% endif %}

<main id="main">
{{ content | safe }}
</main>

{% include "partials/footer.njk" %}
<script src="/js/main.js" defer></script>
</body>
</html>
